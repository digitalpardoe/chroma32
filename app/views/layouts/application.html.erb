<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="utf-8" />
	<title>Chroma32</title>
	<%= stylesheet_link_tag :all %>
	<%= javascript_include_tag :all %>
</head>

<body>
  <% flash.each do |key, msg| %>
		<%= content_tag :div, content_tag(:p, msg), :id => key %>
	<% end %>
	
	<%= yield %>
</body>

</html>
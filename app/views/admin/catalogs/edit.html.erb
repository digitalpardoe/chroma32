<h1>Editing catalog</h1>

<% form_for(@catalog, :url => { :action => 'update' }) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
<% end %>

<%= link_to 'Show', admin_catalog_path(@catalog) %> |
<%= link_to 'Back', admin_catalogs_path %>
